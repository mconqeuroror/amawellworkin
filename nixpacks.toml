[phases.setup]
nixPkgs = [
  'nodejs-18_x',
  'chromium',
  'nss',
  'freetype',
  'freetype-dev',
  'harfbuzz',
  'ca-certificates',
  'ttf-freefont',
  'fontconfig',
  'wqy_zenhei',
  'glib',
  'gtk3',
  'libdrm',
  'libxcomposite',
  'libxdamage',
  'libxrandr',
  'libgbm',
  'libxss',
  'libasound2'
]

[phases.install]
cmds = [
  'npm ci --only=production',
  'npx puppeteer browsers install chrome'
]

[phases.build]
cmds = [
  'echo "Build phase - Chrome should already be installed"'
]

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = 'false'
PUPPETEER_EXECUTABLE_PATH = '/app/.cache/puppeteer/chrome/linux-127.0.6533.88/chrome-linux64/chrome'
NODE_ENV = 'production'

[start]
cmd = 'node glamira_scraper.js'
